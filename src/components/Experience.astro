---
const experiences = [
  {
    role: 'Desarrollador Full Stack Freelance',
    company: 'JMR Marroquinería',
    period: '2026 – Actualidad',
    type: 'Freelance',
    description:
      'Desarrollo y mantenimiento de soluciones digitales completas para optimizar la gestión del negocio. Trabajo continuo en dos productos principales que se mantienen y actualizan actualmente.',
    highlights: [
      'Diseñé y desarrollé un e-commerce completo con catálogo de +1500 productos, carrito de compras, checkout y pasarela de pagos.',
      'Construí un sistema POS (punto de venta) e inventario en tiempo real con alertas automáticas de stock bajo, reportes y soporte multi-usuario.',
      'Integré ambas plataformas para que el control de stock del POS se sincronice con el e-commerce.',
      'Actualmente mantengo y actualizo ambos sistemas: nuevas funcionalidades, carga de imágenes y productos.',
    ],
    techs: ['Next.js', 'React', 'Tailwind', 'Supabase', 'TypeScript', 'SQL', 'Vercel'],
    links: [
      { label: 'Ver E-commerce', href: 'https://www.jmrmarroquineria.com.ar/', icon: 'iconoir-internet' },
      { label: 'GitHub E-commerce', href: 'https://github.com/gonza-rom/jmr-ecommerce', icon: 'iconoir-github' },
      { label: 'GitHub POS', href: 'https://github.com/gonza-rom/jmr-stock', icon: 'iconoir-github' },
    ],
  },
];
---

<section id="experience" class="py-24 bg-white dark:bg-surface-dark">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">

    <h2 class="section-title reveal">Experiencia</h2>

    <div class="space-y-8">
      {experiences.map(exp => (
        <div class="reveal group relative rounded-3xl p-8
                    bg-gray-50 dark:bg-surface-dark-card
                    border border-gray-200 dark:border-surface-dark-border
                    hover:border-accent/40 hover:shadow-xl hover:shadow-accent/10
                    transition-all duration-300">

          <!-- Header row -->
          <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3 mb-5">
            <div>
              <h3 class="text-xl font-bold text-gray-800 dark:text-white">{exp.role}</h3>
              <p class="text-accent font-semibold mt-0.5">{exp.company}</p>
            </div>
            <div class="flex flex-col sm:items-end gap-1.5 flex-shrink-0">
              <span class="inline-flex items-center gap-1.5 text-xs font-semibold px-3 py-1 rounded-full
                           bg-accent/10 text-accent border border-accent/20">
                <i class="iconoir-calendar text-sm"></i>
                {exp.period}
              </span>
              <span class="text-xs font-medium text-gray-500 dark:text-gray-400 px-3">{exp.type}</span>
            </div>
          </div>

          <!-- Description -->
          <p class="text-gray-600 dark:text-gray-400 text-sm leading-relaxed mb-5">{exp.description}</p>

          <!-- Highlights -->
          <ul class="space-y-2.5 mb-6">
            {exp.highlights.map(h => (
              <li class="flex items-start gap-2.5 text-sm text-gray-600 dark:text-gray-400">
                <i class="iconoir-check-circle flex-shrink-0 text-accent text-base mt-0.5"></i>
                <span>{h}</span>
              </li>
            ))}
          </ul>

          <!-- Tech chips -->
          <div class="flex flex-wrap gap-2 mb-5">
            {exp.techs.map(t => (
              <span class="chip">{t}</span>
            ))}
          </div>

          <!-- Links -->
          <div class="flex flex-wrap gap-2">
            {exp.links.map(link => (
              <a
                href={link.href}
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-medium
                       text-gray-600 dark:text-gray-400
                       border border-gray-300 dark:border-gray-600
                       hover:border-accent hover:text-accent dark:hover:border-accent dark:hover:text-accent
                       transition-all duration-200"
              >
                <i class={`${link.icon} text-sm`}></i>
                {link.label}
              </a>
            ))}
          </div>

        </div>
      ))}
    </div>

  </div>
</section>
